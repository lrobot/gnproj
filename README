

this is a simple gn build system, learn from skia 

more detail and doc can ref: https://skia.org/user/build 

thanks skia proj

after git clone this repo

run  ./bin/git-sync-deps  to get some depends sub git repo

simple usage:
  ./bin/buildios.sh
  ./bin/buildmac.sh
  ./bin/buildandroid.sh
  ./bin/buildlinux.sh
  bin\buildwin.bat


